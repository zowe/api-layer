spring.profiles.include: diag

apiml:
    service:
        hostname: localhost
        ipAddress: 0.0.0.0
        port: 10011
    discovery:
        staticApiDefinitionsDirectories: config/local/api-defs
        allPeersUrls: https://localhost:10011/eureka,https://localhost2:10021/eureka
    security:
        ssl:
            verifySslCertificatesOfServices: true
    banner: console

spring:
    output:
        ansi:
            enabled: always

eureka:
    client:
        initialInstanceInfoReplicationIntervalSeconds: 1
        registryFetchIntervalSeconds: 1

server:
    ssl:
        keyAlias: localhost
        keyPassword: password
        keyStoreType: PKCS12
        keyStore: keystore/service/service.keystore.p12
        keyStorePassword: password
        trustStoreType: PKCS12
        trustStore: keystore/service/service.truststore.p12
        trustStorePassword: password
