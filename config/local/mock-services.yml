zosmf:
    timeout: 1800

server:
    ssl:
        keyAlias: localhost
        keyPassword: password
        keyStore: keystore/service/server-only.p12
        keyStorePassword: password
        keyStoreType: PKCS12
        trustStore: keystore/service/service.truststore.p12
        trustStorePassword: password
        trustStoreType: PKCS12

