resourceMetrics:
    -   resource:
            attributes:
                -   key: deployment.environment.name
                    value: { stringValue: "TEST" }
                -   key: service.namespace
                    value: { stringValue: "GITHUB_SERVICE_REGISTRATION_IT" }
                -   key: service.name
                    value: { stringValue: "apiml:localhost:10010" }
                -   key: service.instance.id
                    value: { stringValue: "localhost:gateway:10010" }

        schemaUrl: "https://opentelemetry.io/schemas/1.24.0"
        scopeMetrics:
            -   scope: { "name": "test" }
                metrics: [ { "name": "test.metric", "description": "Synthetic metric for resource attribute validation carrying fixed dummy value", "gauge": { "dataPoints": [ { "asDouble": 1 } ] } } ]
