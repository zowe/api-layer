{
    "$schema": "https://json-schema.org/draft/2019-09/schema",
    "$id": "https://zowe.org/schema/api-layer-config",
    "title": "API Catalog configuration",
    "description": "Properties that can be used to configure API Catalog in a configuration file, such as zowe.yaml.",
    "type": "object",
    "additionalProperties": true,
    "properties": {
        "port": {
            "$ref": "#/$defs/port",
            "default": 7554
        },
        "debug": {
            "type": "boolean",
            "description": "Enable debug logs in discovery service"
        }
    },
    "$defs": {
        "port": {
            "type": "integer",
            "description": "TCP network port",
            "minimum": 1024,
            "maximum": 65535
        }
    }
}
