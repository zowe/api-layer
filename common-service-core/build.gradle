dependencies {
    compileOnly libraries.gson
    compileOnly(libraries.eureka_client) {
        exclude group: "javax.servlet", module: "servlet-api"
        exclude group: "com.google.code.gson", module: "gson"
    }

    compileOnly libraries.netflix_servo

    compileOnly(libraries.http_client)
    compileOnly(libraries.http_core)
    compileOnly(libraries.jackson_annotations)
    compileOnly(libraries.jackson_databind)
    compileOnly(libraries.slf4j_api)
    compileOnly(libraries.snakeyaml)

    compileOnly libraries.apache_commons_codec
    compileOnly libraries.apache_commons_lang3
    compileOnly libraries.apache_commons_text
    compileOnly libraries.xstream
    compileOnly libraries.logback_core
    compileOnly libraries.logback_classic

    compileOnly libraries.jsr305

    compileOnly(libraries.javax_servlet_api)

    compileOnly libraries.lombok
    annotationProcessor libraries.lombok

    compileOnly(libraries.spring_boot_starter_cache)
    compileOnly(libraries.eh_cache)

    testImplementation(libraries.spring_boot_starter_cache)
    testImplementation(libraries.spring_boot_starter_test)
    testImplementation libraries.json_smart
    testImplementation(libraries.eh_cache)
    testImplementation(libraries.jackson_core)
    testImplementation(libraries.jackson_databind)
    testImplementation(libraries.javax_servlet_api)
    testImplementation(libraries.json_unit_fluent)
    testImplementation(libraries.json_unit)
    testImplementation(libraries.hamcrest)
    testImplementation(libraries.logback_classic)
    testImplementation(libraries.tomcat_embed_core)
    testImplementation(libraries.mockito_core)

    testImplementation libraries.lombok
    testAnnotationProcessor libraries.lombok
}
